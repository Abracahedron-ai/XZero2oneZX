# ============================================
# Dependencies (should NOT be in git)
# ============================================
node_modules/
**/node_modules/
**/venv/
**/__pycache__/
**/*.pyc
**/*.pyo
**/*.pyd
.Python
*.so
*.egg
*.egg-info/
.eggs/
pip-log.txt
pip-delete-this-directory.txt

# ============================================
# Build outputs (should NOT be in git)
# ============================================
renderer/.next/
renderer/out/
renderer/build/
dist/
build/
out/
*.tsbuildinfo
.next/
.nuxt/
.cache/
.parcel-cache/

# ============================================
# Large model files (should NOT be in git)
# ============================================
# Model weights and checkpoints
**/*.safetensors
**/*.pt
**/*.pth
**/*.ckpt
**/*.pkl
**/*.h5
**/*.onnx
**/*.pb
**/*.tflite
**/*.bin
Models/**/*.safetensors
Models/**/*.pt
Models/**/*.pth
Models/**/*.ckpt
FBmap/*.safetensors
FBmap/*.pt
FBmap/*.pth

# Large zip/archive files
blender-mcp-main.zip
**/*.zip
**/*.tar.gz
**/*.tar.bz2
**/*.rar

# ============================================
# Logs and runtime data (should NOT be in git)
# ============================================
*.log
logs/
**/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ============================================
# Environment and secrets (should NOT be in git)
# ============================================
.env
.env.local
.env*.local
.env.production
.env.development
*.key
*.pem
*.cert
secrets/
credentials/

# ============================================
# Database files (should NOT be in git)
# ============================================
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
postgres_data/
redis_data/
prometheus_data/
grafana_data/

# ============================================
# Docker volumes and data (should NOT be in git)
# ============================================
docker-data/
volumes/
*.volume

# ============================================
# Experimental/third-party repos (should NOT be in git)
# These are likely cloned repos - use git submodules instead
# ============================================
EXPERIMENTAL/
Deep-Live-Cam/
blender-mcp/
fabric-video-editor/
tsparticles/
# Note: If you want to track some of these, uncomment specific dirs:
# !blender-mcp/your-modified-code/
# Or convert to git submodules:
# git submodule add <repo-url> blender-mcp

# ============================================
# OS files (should NOT be in git)
# ============================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ============================================
# IDE files (should NOT be in git)
# ============================================
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# ============================================
# Electron (should NOT be in git)
# ============================================
electron/dist/
electron-builder.yml
*.asar

# ============================================
# Python (should NOT be in git)
# ============================================
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
cython_debug/

# ============================================
# Testing (should NOT be in git)
# ============================================
coverage/
.nyc_output/
test-results/
playwright-report/

# ============================================
# Temporary files (should NOT be in git)
# ============================================
temp/
tmp/
*.tmp
*.temp
*.bak
*.backup
*.swp
*.swo

# ============================================
# Misc (should NOT be in git)
# ============================================
*.pid
*.seed
*.pid.lock
.npm
.eslintcache
.stylelintcache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/
